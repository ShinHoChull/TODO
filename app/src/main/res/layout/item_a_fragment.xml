<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="vo"
            type="com.example.todo.model.domain.Todo" />

        <variable
            name="vm"
            type="com.example.todo.vm.AViewModel" />

        <variable
            name="item"
            type="com.example.todo.a.viewholder.AViewHolder" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <TextView
            android:id="@+id/text1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:text='@{vo.todoStr == null ? "-" : vo.todoStr }'
            android:textColor="#000000"
            android:textSize="20dp"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="15dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@id/list_check_box"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <CheckBox
            android:id="@+id/list_check_box"
            android:layout_width="30dp"
            android:layout_height="wrap_content"
            android:checked="@{vo.check}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toLeftOf="@id/text1"
            app:layout_constraintBottom_toBottomOf="parent"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>